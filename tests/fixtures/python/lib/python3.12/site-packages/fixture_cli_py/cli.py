import argparse


def main():
    parser = argparse.ArgumentParser(prog="fixture-cli-py")
    parser.add_argument("--sample", action="store_true", help="enable the sample option")
    args = parser.parse_args()
    if args.sample:
        print("sample enabled")
    else:
        print("fixture cli")
